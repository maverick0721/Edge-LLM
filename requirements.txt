# Minimal runtime for the deployed edge chat stack.
# This keeps low-power devices on the smallest practical Python footprint.
fastapi==0.135.1
httpx==0.28.1
pydantic==2.12.5
uvicorn==0.41.0
websockets==16.0

# --- Standard (RAG / ML) and High-quality (voice) extras ---
# The project previously split optional tiers into separate files.
# They are now consolidated here so one `requirements.txt` controls
# all optional tiers. To install a smaller subset, pick packages
# manually or use a virtual environment.

# Numerical + ML
numpy==1.26.4
psutil==7.2.2

# Faiss and embeddings / tokenizers
faiss-cpu==1.13.2
sentence-transformers==5.2.3
tokenizers==0.22.2

# Deep learning / Transformers
torch==2.6.0
transformers==4.57.6

# Voice / audio
pyttsx3==2.99
sounddevice==0.5.5
vosk==0.3.44

